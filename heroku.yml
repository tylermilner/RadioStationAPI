setup:
 addons:
   - plan: heroku-postgresql
build:
 docker:
   web: ./Dockerfile
release:
 image: web
 command:
   - ./release-tasks.sh
run:
 web: Run serve --env production --hostname 0.0.0.0 --port $PORT

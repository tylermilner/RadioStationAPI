setup:
 addons:
   - plan: heroku-postgresql
build:
 docker:
   web: ./Dockerfile
release:
 image: web
 command:
   - ./Run migrate --yes
run:
 web: Run serve --env production --hostname 0.0.0.0 --port $PORT
